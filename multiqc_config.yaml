report_comment: >
  This report has been generated by the <a href="https://github.com/ranlib/whole_genome" target="_blank">ranlib/wholegenome</a>
  analysis pipeline. For information about how to interpret these results, please see the
  <a href="https://github.com/ranlib/wholegenome" target="_blank">documentation</a>.

data_format: "yaml"

max_table_rows: 10000

run_modules:
  - fastqc
  - fastp
  - kraken
  - mosdepth
  - picard
  - snpeff
  
module_order:
  - fastqc:
      name: "PREPROCESS: FastQC (raw reads)"
      anchor: "fastqc"
      info: "FastQC results for the raw reads before adapter trimming."
      path_filters:
        - "*/fastqc_data.txt"
  - fastqc:
      name: "PREPROCESS: FastQC (trimmed reads)"
      anchor: "fastqc_after_clean_up"
      info: "FastQC results for the raw reads before adapter trimming."
      path_filters:
        - "*/fastqc_data.txt"
  - fastp:
      name: "PREPROCESS: fastp (loose trimming)"
      anchor: "fastp_loose"   
      info: " results for reads after adapter and quality trimming."
      path_filters:
        - "./fastp_loose_*.json"
  - fastp:
      name: "PREPROCESS: fastp (tight trimming)"
      anchor: "fastp"   
      info: " results for reads after adapter and quality trimming."
      path_filters:
        - "./fastp_tight_*.json"
  - kraken:
      name: "PREPROCESS: Kraken 2 (tight trimming)"
      info: "Kraken 2 classification results for reads after adapter trimming with fastp."
  - samtools:
      name: "VARIANTS: SAMTools (raw)"
      anchor: "samtools"
      info: "SAMTools counts/statistics after mapping."
      path_filters:
        - "./samtools*"
  - snpeff:
      name: "VARIANTS: snpEff (tight trimming)"
      anchor: "snpEff"
      info: "SNPs annotated vis snpEff."
      path_filters:
        - "./*snpeff_summary.csv"
  - picard:
      name: "BAM QC: picard"
      info: "WGS QC from picard."
  - mosdepth:
      name: "BAM QC: mosdepth"
      info: "This section of the report shows genome-wide coverage metrics generated by mosdepth."
